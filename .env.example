# Resend (contact form → email to you)
# Get API key at https://resend.com
RESEND_API_KEY=
RESEND_TO=xxx@xxcompany.com
# Optional: custom "from" (default: Portfolio <onboarding@resend.dev>)
# RESEND_FROM=Portfolio <onboarding@resend.dev>

# Optional: override contact endpoint (default: /api/contact)
# NEXT_PUBLIC_CONTACT_SERVICE_URL=

# Fallback for mailto: when API is unavailable
NEXT_PUBLIC_CONTACT_EMAIL=xxx@xxcompany.com

# Scheduling: use your Cal.com event URL without query params (e.g. ?overlayCalendar=true can cause 404 in embed).
# e.g. https://cal.com/yourusername/30min
NEXT_PUBLIC_CALENDLY_URL=

# Admin: login password for /admin
ADMIN_PASSWORD=

# Auth: generate with: openssl rand -base64 32
AUTH_SECRET=
# NextAuth: set in production (e.g. https://yoursite.com)
# NEXTAUTH_URL=http://localhost:3000

# Database: for editable portfolio content. Use one of:
# - Neon (preferred): Create project at neon.tech, run neon-schema.sql, set DATABASE_URL
# - Supabase: Create project at supabase.com, run supabase-schema.sql, set vars below
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
