# Optional endpoint for external form/email service (Formspree, Basin, etc.)
# Must accept JSON with: name, email, message
NEXT_PUBLIC_CONTACT_SERVICE_URL=

# Optional fallback destination for mailto: links
NEXT_PUBLIC_CONTACT_EMAIL=mj29@fordham.edu

# Optional scheduling link
NEXT_PUBLIC_CALENDLY_URL=
